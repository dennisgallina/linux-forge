[defaults]
inventory = inventory/hosts.ini
remote_user = dgallina
private_key_file = ~/.ssh/id_ed25519
host_key_checking = False
roles_path = roles

[privilege_escalation]
become = True
become_method = sudo
become_user = root